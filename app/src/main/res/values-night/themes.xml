<resources>
    <!-- Dark mode theme for Compose-based app. -->
    <style name="Theme.IcCardScanner" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
